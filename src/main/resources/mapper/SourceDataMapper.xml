<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.datastream.mapper.SourceDataMapper">

    <resultMap id="BaseResultMap" type="com.datastream.entity.SourceData">
        <id column="id" property="id"/>
        <result column="business_time" property="businessTime"/>
        <result column="user_code" property="userCode"/>
        <result column="user_name" property="userName"/>
        <result column="amount" property="amount"/>
        <result column="status" property="status"/>
        <result column="remark" property="remark"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 根据时间范围查询 -->
    <select id="selectByTimeRange" resultMap="BaseResultMap">
        SELECT * FROM source_data
        WHERE business_time BETWEEN #{startTime} AND #{endTime}
        ORDER BY business_time DESC
    </select>

    <!-- 根据用户编码和时间范围查询 -->
    <select id="selectByUserCodeAndTimeRange" resultMap="BaseResultMap">
        select s.* from
        source_data s
        inner join target_data t
        on s.user_code = t.user_code
        where s.business_time BETWEEN #{startTime} AND #{endTime}
        and s.user_code = #{userCode}
        ORDER BY s.business_time DESC
    </select>

</mapper>
